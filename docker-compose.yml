services:
  postgres:
    image: postgres:16-alpine
    container_name: voidAccess_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: voidAccess
      POSTGRES_PASSWORD: voidAccess
      POSTGRES_DB: voidAccess
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U voidAccess -d voidAccess"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: voidAccess_cache
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:
